[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rabbit"
version = "24.9"
dependencies = ["pika", "python-dotenv"]

[tool.pyright]
reportUnusedImport = true
reportUnusedVariable = true

[tool.ruff]
line-length = 100
